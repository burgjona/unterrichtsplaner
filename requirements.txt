# Laufzeit-Abhängigkeiten (Meilenstein 1 + 2)
fastapi==0.128.8
uvicorn==0.39.0
python-multipart==0.0.20
# pydantic/starlette/anyio kommen transitiv mit fastapi, hier zur Nachvollziehbarkeit gepinnt:
pydantic==2.13.4
starlette==0.49.3
# Auth (M2)
argon2-cffi==25.1.0
cryptography==49.0.0
# Material/PDF-Extraktion (M5)
pypdf==6.14.2
# ASUV-Export (M6): Word + PDF
python-docx==1.2.0
reportlab==5.0.0
# Claude-API (M7)
anthropic==0.116.0
# Google-Kalender-Sync (U21): Service-Account-Auth + REST via httpx.
# httpx wurde von den Dev-Deps hierher hochgezogen (Laufzeit-Nutzung im google_cal-Client).
google-auth==2.38.0
requests==2.32.5          # Transport für google-auth Credentials.refresh(Request())
httpx==0.28.1
# Web-Push-Benachrichtigungen: Nachrichtenverschlüsselung (RFC 8291) + VAPID (RFC 8292).
pywebpush==2.1.2
# Verschlüsselte Datensicherung + Noten-Exporte (U29): AES-256-ZIP (WinZip-AES) und der
# Office-eigene Passwortschutz für .docx (nutzt das oben gepinnte cryptography).
pyzipper==0.4.0
msoffcrypto-tool==5.4.2
# Zeitzonendaten für zoneinfo (Europe/Berlin): das slim-Image bringt sie nicht sicher mit,
# der Container läuft sonst in UTC und Erinnerungen kämen eine/zwei Stunden daneben.
tzdata==2026.3
